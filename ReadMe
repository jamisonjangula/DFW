# step 1) run dfw.py in the folder containing the original data spreadsheet. This will create DFW_valuable.csv
# step 2) run flightAnalysis.py to detect dropouts based on iterative outlier detection and to produce videos of flight paths
